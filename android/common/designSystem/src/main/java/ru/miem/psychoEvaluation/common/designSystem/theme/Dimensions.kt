package ru.miem.psychoEvaluation.common.designSystem.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

object Dimensions {
    val primaryHorizontalPadding = 24.dp
    val primaryVerticalPadding = 24.dp

    val secondaryVerticalPadding = primaryVerticalPadding / 2
    val secondaryHorizontalPadding = primaryHorizontalPadding / 2

    val commonPadding = 16.dp
    val commonSpacing = 10.dp

    val dividerThickness = 1.dp

    val ripplePadding = 8.dp
    val rippleShape = RoundedCornerShape(percent = 20)
}
